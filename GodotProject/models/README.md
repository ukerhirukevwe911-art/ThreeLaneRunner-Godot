# Minimal Mesh fallback model: MeshInstance3D using a BoxMesh
# Player.gd will configure this node at runtime if an external model isn't present.
# This avoids a missing dependency when the external GLB is not available.
